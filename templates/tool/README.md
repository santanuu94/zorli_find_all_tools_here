# Tool Name

Isolated feature module description.

## Purpose
Explains what the tool does and why it was built.

## Supported Inputs
List valid inputs, mime-types, and constraints.

## Architecture
- `config.ts`: Metadata, tags, how-it-works, FAQs
- `components/`: Tool-specific UI components
- `lib/`: Business logic, engines, validators
- `hooks/`: State and lifecycle hooks
- `utils/`: Specific formatters or calculators
